{
  "name": "native-type-script",
  "version": "1.0.0",
  "description": "This project contains a native TypeScript frontend and backend.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build-backend && npm run copy-backend-to-dist && npm run build-frontend && npm run copy-frontend-to-dist",
    "build-backend": "cd backend && tsc",
    "build-frontend": "cd frontend && tsc",
    "copy-backend-to-dist": "copyfiles backend/package.json dist && cd dist/backend && npm install --only=production",
    "copy-frontend-to-dist": "copyfiles frontend/src/index.html dist",
    "start": "ts-node ./backend/src/server.ts",
    "start-prod": "node ../dist/backend/src/server.js"
  },
  "keywords": [],
  "author": "Roel van Lisdonk",
  "license": "ISC"
}
